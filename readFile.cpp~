#include "node-info.h"
#include <iostream>
#include <string>
#include <fstream>
#include <vector>

// returns 1 on success and 0 on failure
int read_file(string filename, vector<nodeinfo> &nodevec, string source)
{
  ifstream fin;
  fin.open(filename.c_str());

  string line;
  
  while ( getline(fin, line) )
    {
      NodeInfo n;    
      int last_delim_index = 0;
      
      for (int i = 0; i < line.length(); i++)
	{
	  if (line[i] == ',')
	    {
	      if (field == 0)
		{
		  if (line.substr(0,i) != source)
		    {
		      last_delim_index = -1;
		      break;
		    }
		}
	      else if (field == 1)
		{
		  NodeInfo.dest_router = 
		  
		}
	      
	      field++;
	      last_delim_index = i;
	    }
	}

      if (last_delim_index != -1)
	nodevec.push_back(NodeInfo);
    }

  





}
